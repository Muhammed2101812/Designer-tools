# ============================================
# DESIGN KIT - Production Environment Variables
# ============================================
#
# Instructions:
# 1. Copy this file to .env.production: cp .env.production.example .env.production
# 2. Fill in your actual production values
# 3. NEVER commit .env.production to git
# 4. Use these values in your hosting platform's environment settings
#
# Security Notes:
# - All values must be production-ready
# - Use HTTPS URLs only
# - Use Stripe live keys (pk_live_, sk_live_)
# - Keep all secrets secure and never expose them
#
# ============================================

# --------------------------------------------
# Application Configuration (REQUIRED)
# --------------------------------------------

# Production App URL (MUST use HTTPS)
NEXT_PUBLIC_APP_URL=https://your-domain.com
NEXT_PUBLIC_APP_NAME="Design Kit"

# Environment (MUST be production)
NODE_ENV=production

# --------------------------------------------
# Supabase Production (REQUIRED)
# --------------------------------------------
# Get these from: https://app.supabase.com/project/_/settings/api

# Supabase Production Project URL
NEXT_PUBLIC_SUPABASE_URL=https://your-project-id.supabase.co

# Supabase Production Anonymous Key
NEXT_PUBLIC_SUPABASE_ANON_KEY=eyJhbGciOiJIUzI1NiIsInR5cCI6IkpXVCJ9.your-production-anon-key

# Supabase Production Service Role Key (SERVER-SIDE ONLY)
SUPABASE_SERVICE_ROLE_KEY=eyJhbGciOiJIUzI1NiIsInR5cCI6IkpXVCJ9.your-production-service-role-key

# --------------------------------------------
# Stripe Production (REQUIRED - LIVE MODE ONLY)
# --------------------------------------------
# Get these from: https://dashboard.stripe.com/apikeys
# IMPORTANT: Must use LIVE keys (pk_live_, sk_live_)

# Stripe Live Publishable Key
NEXT_PUBLIC_STRIPE_PUBLISHABLE_KEY=pk_live_your_live_publishable_key

# Stripe Live Secret Key (SERVER-SIDE ONLY)
STRIPE_SECRET_KEY=sk_live_your_live_secret_key

# Stripe Webhook Secret (SERVER-SIDE ONLY)
# Get from: https://dashboard.stripe.com/webhooks
# Endpoint: https://your-domain.com/api/stripe/webhook
STRIPE_WEBHOOK_SECRET=whsec_your_webhook_secret

# Stripe Live Price IDs (from Stripe Dashboard → Products)
STRIPE_PREMIUM_PRICE_ID=price_your_premium_price_id
STRIPE_PRO_PRICE_ID=price_your_pro_price_id

# --------------------------------------------
# Upstash Redis Production (RECOMMENDED)
# --------------------------------------------
# Get from: https://console.upstash.com/
# Required for: Rate limiting, session caching

# Upstash Redis Production URL
UPSTASH_REDIS_URL=https://your-redis-id.upstash.io

# Upstash Redis Production Token
UPSTASH_REDIS_TOKEN=your_production_redis_token

# --------------------------------------------
# Sentry Production (RECOMMENDED)
# --------------------------------------------
# Get from: https://sentry.io/settings/projects/
# Required for: Error tracking and monitoring

# Sentry Production DSN
NEXT_PUBLIC_SENTRY_DSN=https://your-dsn@your-org.ingest.sentry.io/your-project-id

# Sentry Auth Token (for source maps upload)
SENTRY_AUTH_TOKEN=sntrys_your_auth_token

# Sentry Organization Slug
SENTRY_ORG=your-org-slug

# Sentry Project Name
SENTRY_PROJECT=design-kit

# Sentry Environment
SENTRY_ENVIRONMENT=production

# --------------------------------------------
# Resend Production (RECOMMENDED)
# --------------------------------------------
# Get from: https://resend.com/api-keys
# Required for: Transactional emails

# Resend Production API Key
RESEND_API_KEY=re_your_production_api_key

# From Email Address (must be verified in Resend)
EMAIL_FROM=noreply@your-domain.com

# --------------------------------------------
# External API Services (OPTIONAL)
# --------------------------------------------

# Remove.bg API Key (Background Remover Tool)
# Get from: https://www.remove.bg/api
REMOVE_BG_API_KEY=your_removebg_api_key

# Replicate API Key (Image Upscaler Tool)
# Get from: https://replicate.com/account/api-tokens
REPLICATE_API_KEY=your_replicate_api_key

# --------------------------------------------
# Production Security Settings
# --------------------------------------------

# Disable telemetry in production
NEXT_TELEMETRY_DISABLED=1

# Enable rate limiting
RATE_LIMIT_ENABLED=true

# Production logging level
LOG_LEVEL=error

# ============================================
# DEPLOYMENT CHECKLIST
# ============================================
#
# Before deploying to production, ensure:
# ☐ All REQUIRED variables are filled
# ☐ Using HTTPS URLs only
# ☐ Using Stripe LIVE keys (not test keys)
# ☐ Supabase production project is set up
# ☐ Database migrations are run
# ☐ Stripe webhook endpoint is configured
# ☐ Domain is verified in Resend (if using email)
# ☐ Sentry project is created (if using error tracking)
# ☐ Redis database is created (if using rate limiting)
# ☐ All secrets are kept secure
#
# Validation Commands:
# npm run validate:env          # Validate environment variables
# npm run deploy:setup          # Run production deployment setup
# npm run verify:production     # Verify production deployment
#
# ============================================
# SECURITY REMINDERS
# ============================================
#
# ⚠️  NEVER commit this file to git
# ⚠️  Add .env.production to .gitignore
# ⚠️  Use different keys for development/production
# ⚠️  Rotate keys if accidentally exposed
# ⚠️  Keep service role keys server-side only
# ⚠️  Use environment variables in hosting platform
# ⚠️  Monitor for unauthorized access
# ⚠️  Regularly audit and update credentials
#
# ============================================